services:
  - name: cbr2epub-web
    env: python
    buildCommand: apt-get update && apt-get install -y unrar && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free